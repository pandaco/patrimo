# web-features

Route-level pages (standalone, OnPush, signals) : dashboard, wealth (patrimoine), portfolio, liabilities (passifs), transactions, allocation, performance, analyses (indicators / cashflow / projection), report (bilan), dca, calendar, compare, alerts, glossary, tips, preferences, welcome, login. Wired to `web-data-access` services and rendered inside the `apps/web` shell.
